using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;
using Backend.Data;

namespace Backend.Controllers
{
    // A generic controller could be complex to implement perfect with EF Core in one go without creating a Repo pattern.
    // For this prototype, I will create individual controllers but follow a strict pattern.
    // I will generate: RazonSocialController, BasculasController, UsuariosController.
}
